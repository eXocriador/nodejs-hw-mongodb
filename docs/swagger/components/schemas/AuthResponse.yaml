type: object
required:
  - status
  - message
  - data
properties:
  status:
    type: integer
    example: 200
  message:
    type: string
    example: "Successfully authenticated!"
  data:
    type: object
    properties:
      accessToken:
        type: string
        description: JWT токен доступу
